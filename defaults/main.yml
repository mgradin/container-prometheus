---
# defaults file for container-prometheus
VAR_PROMETHEUS_DIR: /opt/prometheus
VAR_PROMETHEUS_URL: prometheus.example.com
VAR_NODE_EXPORTER_SERVERS: ['server01:9100', 'server02:9100']
VAR_INFLUXDB_REMOTE_WRITE: "http://localhost:8086/api/v1/prom/write?db=prometheus"
